# Normalize every text file to LF in the repository and in the working tree.
# api.py and four other modules were previously stored with CRLF, which turned
# any edit made by a newline-normalizing tool into a whole-file rewrite and
# buried the real change in the diff.
* text=auto eol=lf
